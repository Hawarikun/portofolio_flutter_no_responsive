import 'package:flutter/material.dart';

class ColorsApp {
  // Primary Color
  static const primaryColor = Color.fromRGBO(39, 39, 39, 1);

  // Text Color
  static const textColor = Color.fromRGBO(255, 255, 255, 1);
}
